<%= image_tag "annotated_award_img.png" %>

<%= form_for @award do |f| %>
  <div class="actions">
    <%= f.submit '投稿画面に戻る', name: 'back' %>
  </div>
  <div class="actions">
    <%= f.submit '賞を送る' %>
  </div>
<% end %>