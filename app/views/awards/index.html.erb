<p id="notice"><%= notice %></p>

<h1>Awards</h1>
このアプリはこういうものです

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Username</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.username %></td>
        <td><%= link_to 'Show', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>



<% if user_signed_in? %>
  <%= link_to 'マイページ', user_path(current_user) %>
<% else %>
  <%= link_to 'マイページを見るためにログインする', new_user_registration_path %>
<% end %>

